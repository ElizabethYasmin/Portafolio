name: portafolio_yasmin
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.1
  provider: ^6.1.2
  flutter_dotenv: ^5.2.1
  flutter_bloc: ^9.0.0
  google_fonts: ^6.2.1
  get_it: ^8.0.3
  shared_preferences_web: ^2.0.1 #NO ESTA
  shared_preferences: ^2.4.0
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/profile.png
